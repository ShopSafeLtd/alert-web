query EditBusiness($where: BusinessWhereUniqueInput!) {
  business(where: $where) {
    id
    name
    fullName
    publicName
    siteNumber
    parent {
      id
      name
      fullName
    }
    groups {
      id
      name
    }
    tags {
      id
      name
    }
    locations {
      id
      building
      street
      townCity
      county
      postcode
      geoLng
      geoLat
    }
  }
}
