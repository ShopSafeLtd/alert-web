query PendingLoginPromptVideos($schemeId: String!) {
  pendingLoginPromptVideos(schemeId: $schemeId) {
    id
    title
    description
    videoUrl
    thumbnailUrl
    duration
    mandatory
    loginPrompt
  }
}
