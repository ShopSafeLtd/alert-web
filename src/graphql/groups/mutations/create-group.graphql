mutation createGroup($data: GroupCreateInput!) {
  createGroup(data: $data) {
    id
    name
    description
    usersCount
    businessesCount
    approver {
      id
      fullName
    }
    scheme {
      id
      name
    }
  }
}
