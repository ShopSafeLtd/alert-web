mutation createGroup($data: GroupCreateInput!) {
  createGroup(data: $data) {
    id
    name
    description
    users {
      id
      fullName
    }
    approver {
      id
      fullName
    }
    scheme {
      id
      name
    }
  }
}
