mutation CreateSimpleVehicle($data: CreateVehicleDataInput!) {
  createVehicle(data: $data) {
    id
    reference
    colour
    model
    make
    registration
    images(take: 1) {
      id
      url
      policeImage
      primary
      position
      rotation
      optimised
    }
  }
}
