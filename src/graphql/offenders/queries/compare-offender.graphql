query ViewOffenderCompare($where: OffenderWhereUniqueInput!) {
  offender(where: $where) {
    id
    updatedAt
    age
    build
    height
    dateOfBirth
    dateSource
    hair
    gender
    name
    race
    peculiarities
    images {
      id
      optimised
      position
      rotation
    }
    tags {
      id
      name
    }
    lastActive {
      id
      dayTime
    }
  }
}
