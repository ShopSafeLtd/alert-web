mutation updateOffenderImages($id: String!, $images: UpdateOffenderImage) {
  updateOffender(where: { id: $id }, data: { images: $images }) {
    id
    images {
      ...Images
      optimisticUri
    }
  }
}
