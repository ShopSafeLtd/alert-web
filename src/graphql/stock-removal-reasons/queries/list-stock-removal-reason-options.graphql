query ListStockRemovalReasonOptions($where: SchemeWhereUniqueInput!) {
    scheme(where: $where) {
        id
        stockRemovalReasonOptions {
            id
            label
            position
            active
        }
    }
}
