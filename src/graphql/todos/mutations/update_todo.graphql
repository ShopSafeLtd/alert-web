mutation UpdateTodo($data: TodoUpdateInput!, $where: UniqueId!) {
  updateTodo(data: $data, where: $where) {
    type
    description
    dueDate
    completedDate
    completed
    authorised
    id
    name
    similarOffenderIds
    createdBy {
      id
      fullName
    }
    completedBy {
      id
      fullName
    }
    assignedUsers {
      id
      fullName
    }
  }
}
