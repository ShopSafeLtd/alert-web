query PendingCriticalBulletins($schemeId: String!) {
  pendingCriticalBulletins(schemeId: $schemeId) {
    id
    title
    previewText
    previewImage
    criticalExpiry
    priority
    createdAt
    createdBy {
      id
      fullName
    }
  }
}
