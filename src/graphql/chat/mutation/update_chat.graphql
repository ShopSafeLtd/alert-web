mutation updateChat($where: UniqueId!, $data: ChatUpdateInput!) {
  updateChat(where: $where, data: $data) {
    id
    name
    description
    members {
      user {
        id
        fullName
        businesses {
          fullName
          id
          name
        }
      }
    }
  }
}
