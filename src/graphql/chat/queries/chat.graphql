query Chat($where: ChatWhereUniqueInput!) {
  chat(where: $where) {
    id
    name
    description
    members {
      id
      user {
        id
        fullName
        organisation
      }
    }
  }
}
