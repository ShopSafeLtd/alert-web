query Chat($where: ChatWhereUniqueInput!) {
  chat(where: $where) {
    id
    name
    description
    totalMembers
    members {
      id
      user {
        id
        fullName
        businesses {
          fullName
          id
          name
        }
        firstLetter
      }
    }
  }
}
