query Chat($where: ChatWhereUniqueInput!) {
  chat(where: $where) {
    id
    name
    description
    totalMembers
    members {
      id
      user {
        id
        fullName
        businesses {
          id
          name
        }
        firstLetter
      }
    }
  }
}
