query scheme($where: SchemeWhereUniqueInput!) {
  scheme(where: $where) {
    id
    name
    autoApproveIncidents
    autoApproveOffenders
    incidentRetention
    offenderRetention
    logo {
      id
      url
      optimised
    }
  }
}
