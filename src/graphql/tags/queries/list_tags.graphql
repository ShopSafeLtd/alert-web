query ListTags(
  $where: TagWhereInput!
  $order: TagOrderByWithRelationInput
) {
  listTags(where: $where, order: $order) {
    tags {
      id
      name
      description
      crimeType
      type
    }
    total
  }
}
