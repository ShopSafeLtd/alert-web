mutation CreateDocument($data: CreateDocument!) {
  createDocument(data: $data) {
    id
    name
    tags {
      id
      name
    }
    thumbnailUrl
    url
    createdAt
    updatedAt
    folder {
      id
      name
    }
  }
}
