query listDocumentsOnScheme($where: SchemeWhereUniqueInput!) {
    scheme(where: $where) {
        documents {
            id
            name
            tags {
                name
                id
            }
            url
            thumbnailUrl
            createdAt
        }
    }
}