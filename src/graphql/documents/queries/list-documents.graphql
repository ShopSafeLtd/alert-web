query listDocumentsOnScheme($where: SchemeWhereUniqueInput!) {
  scheme(where: $where) {
    mg11Available
    documents {
      id
      name
      tags {
        name
        id
      }
      url
      thumbnailUrl
      createdAt
    }
  }
}
