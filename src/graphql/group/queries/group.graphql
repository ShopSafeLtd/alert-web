query Group($where: GroupWhereUniqueInput!) {
  group(where: $where) {
    id
    name
    description
    users {
      id
      fullName
      businesses {
        fullName
        id
        name
      }
    }
    approver {
      id
      fullName
    }
  }
}
