mutation updateGroup($where: UniqueId!, $data: GroupUpdateInput!) {
  updateGroup(where: $where, data: $data) {
    id
    name
    description
    users {
      id
      fullName
      businesses {
        id
        name
      }
    }
  }
}
