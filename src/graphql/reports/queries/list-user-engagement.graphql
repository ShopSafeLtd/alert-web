query UserEngagement($where: UserContributionWhereInput!) {
  listUserContribution(where: $where) {
    userContributions {
      name
      businesses
      totalIncidents
      totalOffenders
      totalUpdates
      totalMessages
      totalLogins
      lastLogin
      groups
    }
    total
  }
}
