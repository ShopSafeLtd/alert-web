query ListBusinessesSelect(
  $where: BusinessWhereInput
  $skip: Int
  $take: Int
  $orderBy: [BusinessOrderBy!]
) {
  businessRelay(where: $where, skip: $skip, take: $take, orderBy: $orderBy) {
    totalCount
    edges {
      node {
        id
        name
        locations {
          id
          full
        }
      }
    }
  }
}
