query demDevicesSelect($where: DemDeviceWhereInput) {
  demDevices(where: $where) {
    edges {
      node {
        id
        name
      }
    }
    totalCount
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
