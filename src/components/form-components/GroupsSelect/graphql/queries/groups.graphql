query schemeGroupsSelect($where: UserGroupsReplayWhere) {
  userGroupRelay(where: $where) {
    edges {
      node {
        id
        name
        scheme {
          id
          name
        }
      }
    }
    totalCount
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
