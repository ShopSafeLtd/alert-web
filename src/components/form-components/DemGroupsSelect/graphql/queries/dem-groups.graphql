query demGroupsSelect($where: DemGroupWhereInput) {
  demGroups(where: $where) {
    edges {
      node {
        id
        name
      }
    }
    totalCount
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
