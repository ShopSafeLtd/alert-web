query EditReportTemplate($where: ReportTemplateWhereUniqueInput!) {
    reportTemplate(where: $where) {
        id
        name
        description
        type
        reportGroup {
            id
        }
        groups {
            id
            name
        }
    }
}
