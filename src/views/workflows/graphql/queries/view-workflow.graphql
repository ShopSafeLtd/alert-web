query ViewWorkflow($where: WorkflowWhereUniqueInput!) {
  workflow(where: $where) {
    name
    conditions
    triggerModels
      cronSchedule
      cronDate

    actions {
      data
      id
    }
  }
}
