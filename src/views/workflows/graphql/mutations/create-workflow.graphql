mutation CreateOneWorkflow($data: WorkflowCreateInput!) {
  createOneWorkflow(data: $data) {
    id
    name
    trigger
    triggerModels
    actions {
      type
      outputModel
      timesRun
    }
  }
}
