query BusinessDetails($where: BusinessWhereUniqueInput!) {
  business(where: $where) {
    id
    name
    fullName
    locations {
      id
      building
      street
      townCity
      county
      postcode
    }
  }
}
