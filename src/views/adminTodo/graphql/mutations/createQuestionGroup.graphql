mutation CreateOneQuestionGroup($data: QuestionGroupCreateInput!) {
  createOneQuestionGroup(data: $data) {
    id
    name
    description
    defaultDueDate
    questions {
      questionFormatted
      id
    }
  }
}
