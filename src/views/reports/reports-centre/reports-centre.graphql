query ReportsCentre($where: ReportsCentreWhereInput!) {
    reportsCentre(where: $where) {
        id
        name
        order
        reports {
            id
            name
            description
            type
        }
    }
}
