query VisionCamerasSelect($schemeIds: [String!]!) {
    aiVisionCameras(where: {schemeIds: $schemeIds}) {
        edges {
            node {
                id
                serialNumber
                make
                model
            }
        }
        totalCount
    }
}
