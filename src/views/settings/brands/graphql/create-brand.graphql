mutation CreateBrand($data: CreateBrandInput!) {
  createBrand(data: $data) {
    id
    name
    description
    businesses {
      id
      name
      siteNumber
      fullName
    }
    scheme {
      id
      name
    }
  }
}
