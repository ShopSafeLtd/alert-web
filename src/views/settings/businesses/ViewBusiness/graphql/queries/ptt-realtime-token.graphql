query PttRealtimeToken($schemeId: String!) {
  scheme(where: { id: $schemeId }) {
    pttRealtimeToken {
      accessToken
      supabaseUrl
      anonKey
      expiresIn
      recordingChannel
      presenceChannel
    }
  }
}
