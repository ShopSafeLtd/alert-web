mutation ApprovePAPStockRequest($where: UniqueId!) {
    approvePAPStockRemovalRequest(where: $where) {
        id
        status
        approvers {
            id
            status
            user {
                id
                fullName
            }
        }
    }
}
